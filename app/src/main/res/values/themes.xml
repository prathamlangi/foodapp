<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodApp" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FoodApp" parent="Base.Theme.FoodApp" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
    </style>
    <style name="NOTitle" parent="Theme.AppCompat.NoActionBar">
    </style>
    <style name="CustomSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@drawable/search_background</item>
        <item name="queryHint">@string/search_hint</item>
    </style>

</resources>